let makeUrl = (name, url) => {
    return {
        name: name,
        url: url
    }
}

export let urls = [
    makeUrl('home', '/'),
    makeUrl('style', '/style'),
    makeUrl('hire', '/hire')
]



